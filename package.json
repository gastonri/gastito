{
  "name": "gastito",
  "version": "1.0.0",
  "description": "Telegram bot para registrar gastos con IA",
  "main": "src/server.ts",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch src/server.ts",
    "dev:staging": "bun --env-file=.env.staging --watch src/server.ts",
    "dev:production": "bun --env-file=.env.production --watch src/server.ts",
    "redis:start": "docker-compose up -d redis",
    "redis:stop": "docker-compose down",
    "redis:logs": "docker-compose logs -f redis",
    "test": "bun test",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "format": "prettier --write \"src/**/*.{ts,json}\"",
    "format:check": "prettier --check \"src/**/*.{ts,json}\"",
    "deploy": "bash scripts/deploy.sh deploy -a gastito",
    "deploy:logs": "bash scripts/deploy-logs.sh",
    "deploy:status": "bash scripts/deploy-status.sh"
  },
  "keywords": [
    "telegram",
    "bot",
    "budget",
    "finance",
    "ai",
    "gemini"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2",
    "express": "^4.18.2",
    "googleapis": "^128.0.0",
    "ioredis": "^5.9.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^8.51.0",
    "@typescript-eslint/parser": "^8.51.0",
    "eslint": "^8.54.0",
    "eslint-config-prettier": "^9.1.2",
    "prettier": "^3.7.4",
    "typescript": "^5.3.2"
  }
}
